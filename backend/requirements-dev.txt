# Full local development requirements (includes heavy ML libs)
# Install with: pip install -r requirements-dev.txt

# Everything in the production requirements
-r requirements.txt

# NLP / AI (large — NOT included in Vercel bundle; app falls back gracefully)
spacy>=3.8,<4
scikit-learn>=1.6
sentence-transformers>=4.0

# Testing
pytest>=8.3
httpx>=0.28
