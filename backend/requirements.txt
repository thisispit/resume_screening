# Web framework
fastapi>=0.115
uvicorn>=0.34
python-multipart>=0.0.20

# Database
SQLAlchemy>=2.0.40
alembic>=1.15
psycopg[binary]>=3.2

# Validation / settings
pydantic>=2.11
pydantic-settings>=2.8
email-validator>=2.2

# Auth
PyJWT>=2.10
bcrypt>=4.3

# Resume parsing
PyMuPDF>=1.26
python-docx>=1.1

# HTTP client (used internally)
httpx>=0.28

# NOTE: sentence-transformers, spacy, and scikit-learn are intentionally
# excluded here because they pull in PyTorch + CUDA (~5 GB) which exceeds
# Vercel's 225 MB function bundle limit. The app automatically falls back to
# fast fuzzy-string matching (SequenceMatcher) and the heuristic parser when
# these libraries are unavailable — functionality is fully preserved.
# Install them locally (backend/requirements.txt) for local dev with full AI.
